import settings
import geometry as geo
import signals as sig
import source
import numpy as np

class Antenna:
	def calculate_signal(self, Source, Time):
		print "Fatal Error: This object does not hold implementations for the Antenna class.";
		exit;
